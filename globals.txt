@tailwind base;
@tailwind components;
@tailwind utilities;

/* ==================== BASE ==================== */
body {
  background-color: #0A0A0F;
  color: white;
}

/* ==================== SCROLLBAR ==================== */
::-webkit-scrollbar {
  width: 6px;
}

::-webkit-scrollbar-track {
  background: #0A0A0F;
  border-radius: 20px;
}

::-webkit-scrollbar-thumb {
  background: #ff2d95;
  border-radius: 20px;
}

::-webkit-scrollbar-thumb:hover {
  background: #ff6bcb;
}

/* Firefox scrollbar */
* {
  scrollbar-width: thin;
  scrollbar-color: #ff2d95 #0A0A0F;
}

/* ==================== SELECTION ==================== */
::selection {
  background: #ff2d95;
  color: white;
}

/* ==================== DEX GLOW ==================== */
.dex-glow {
  box-shadow: 0 0 40px rgba(255, 45, 149, 0.12);
}

.dex-card-hover {
  transition: border-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s ease;
}

.dex-card-hover:hover {
  box-shadow: 0 0 30px rgba(255, 45, 149, 0.15);
}

@media (max-width: 768px) {
  button, a {
    min-height: 44px;
  }
}
